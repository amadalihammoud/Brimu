# 🐳 Variáveis de Ambiente para Docker
# Configure essas variáveis para deploy em produção

# Aplicação
NODE_ENV=production
LOG_LEVEL=info

# Banco de Dados MongoDB
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=change-this-password-in-production
MONGO_DB=brimu

# JWT e Segurança (GERAR NOVOS EM PRODUÇÃO)
JWT_SECRET=generate-secure-key-with-npm-run-generate-secrets

# Email (CONFIGURAR COM CREDENCIAIS REAIS)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=noreply@yourcompany.com

# CORS (CONFIGURAR COM DOMÍNIOS REAIS)
CORS_ORIGIN=http://localhost:3000,http://localhost:3002,https://yourdomaim.com

# Frontend
REACT_APP_API_URL=http://localhost:5000/api
REACT_APP_APP_NAME=Brimu

# Website Next.js
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Configurações opcionais
REDIS_PASSWORD=
BACKUP_ENCRYPTION_KEY=
WEBHOOK_SECRET=